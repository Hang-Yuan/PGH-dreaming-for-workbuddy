---
title: WorkBuddy 原生自动化任务访谈
type: deployment-contract
version: 2.0
status: active
updated: 2026-08-04
---

# WorkBuddy 原生自动化任务访谈

## 范围

记忆系统只接 WorkBuddy 原生自动化任务。部署流程不得创建操作系统任务、无界面命令行包装器或第二套调度状态。

系统只保存一条每日自动化任务：

```text
每日做梦（`daily-dream`）→ 周日条件转周级做梦（`weekly-dream`）→ 季度点条件转季度归档检测（`quarterly-archive detect`）
```

周段和季度段都没有独立自动化任务。季度归档执行（`quarterly-archive execute`）仍需用户当前会话的 C 级授权。

## 三问

一次问一个，答完后给出完整提案并等待确认。

1. **作息**：通常几点睡、几点起？
2. **时区**：当前 IANA 时区是什么，例如 `Asia/Shanghai`？
3. **WorkBuddy 可用性**：夜间是否保持电脑与 WorkBuddy 的本地执行环境可用？

自动化任务不负责关机、唤醒、合盖策略或系统电源管理。机器休眠、关机、应用退出和断网都可能造成缺勤，`week-sync` 负责次日首个真人会话的断档恢复。

## 推算逻辑日界线

日界线应落在用户通常睡眠区间内，使用整点。模板默认 `06:00` 只用于未初始化状态。

推算顺序：

1. 若 `04:00` 位于睡眠区间，优先提议 `04:00`。
2. 若不在，选择睡眠区间内最接近中点的整点。
3. 固化时刻固定为日界线后 30 分钟。
4. 把「日界线、固化时刻、IANA 时区」念给用户确认。
5. 用户修正后，以用户明确值为准。

确认后同步两处本地权威口径：

- `.pgh/state.json`
- `runtime/workspace/MEMORY/00.memory_agent.md §逻辑日期`

两处边界小时必须相同。公开 `CODEBUDDY.md` 只保留模板示例；SessionStart hook 从本地状态注入当前实例的 IANA 时区与边界。

## 创建或更新唯一自动化任务

通过 WorkBuddy 原生自动化任务管理能力执行。禁止直接编辑自动化任务的 TOML 或状态数据库。

自动化任务合同：

| 字段 | 值 |
|---|---|
| 类型（`kind`） | 独立项目自动化任务 |
| 标识（`id`） | 更新既有任务时保留原标识 |
| 名称（`name`） | 每日做梦（`daily-dream`） |
| 提示词（`prompt`） | `使用 $daily-dream 处理最近一个已经闭合的逻辑日；严格执行事务闸，并报告提交收据。` |
| 周期（`cadence`） | 每天，日界线后 30 分钟 |
| 项目（`project`） | 当前仓库根目录 |
| 执行环境（`execution`） | `local` |
| 状态（`status`） | 用户确认后 `ACTIVE` |
| 模型 / 推理强度 | 沿用用户为该任务明确确认的组合 |

更新前先查看现有自动化任务。命中 `daily-dream` 时原位更新；失败时保留旧任务并回报，不创建重复任务。

保存后回读，核对：自动化任务标识、名称、`ACTIVE` 状态、每日时刻、项目、模型、推理强度、`local` 环境和精确提示词。

## 首跑验收

配置验收与运行验收分开：

- **配置已保存**：WorkBuddy 返回更新成功，回读同一自动化任务。
- **日链已运行**：WorkBuddy 自动化任务历史出现该任务的计划触发终态。
- **事务已闭合**：目标日 `dream_receipts/YYYY-MM-DD.json` 为 `COMMITTED`，`last_dream.md` 指向同一天，覆盖收据与决策收据通过。

立即运行只验证日链本身。计划触发需要等待自动化任务历史出现计划触发运行。

首跑失败时：

1. 保留失败运行与梦收据现场。
2. 不推进 `last_dream.md`。
3. 修复日链或自动化任务定义后重试同一逻辑日。
4. 次日 `week-sync` 截取最近三个有效工作日的可救窗口，并在窗口内按最早缺日优先补跑；窗口以前的信号按接受丢失披露。

## 改作息

改作息时更新原自动化任务：

1. 重新确认日界线、固化时刻和时区。
2. 同步 `.pgh/state.json` 与运行工作区的边界小时和 IANA 时区。
3. 通过 WorkBuddy 自动化任务管理能力更新同一任务。
4. 回读并确认没有第二条每日、周日或季度自动化任务。

## 机械闸

- 现役发布包不得包含 OS 调度安装脚本或包装器。
- README、宪法层、技能与架构文档必须把 WorkBuddy 原生自动化任务写成唯一自动入口。
- 每日做梦（`daily-dream`）必须拥有事务与探针；周级做梦（`weekly-dream`）、季度归档（`quarterly-archive`）不拥有排程。
- 首跑成功必须由 WorkBuddy 自动化任务历史和 `COMMITTED` 地面收据共同证明。
